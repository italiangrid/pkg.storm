# Components list - Default value is the complete list
COMPONENTS_centos6="storm-webdav storm-gridftp-dsi storm-xmlrpc-c storm-frontend-server storm-globus-gridftp-mp storm-backend-mp storm-frontend-mp storm-backend-server yaim-storm"
COMPONENTS_centos7="storm-webdav storm-gridftp-dsi storm-xmlrpc-c storm-frontend-server storm-globus-gridftp-mp storm-backend-mp storm-frontend-mp storm-backend-server storm-dynamic-info-provider"

# Set to 1 if you want to skip the pull of the build image
#SKIP_PKG_BASE_PULL_IMAGE=1

# Use DOCKER_REGISTRY_HOST if USE_DOCKER_REGISTRY is true
#USE_DOCKER_REGISTRY=false
#DOCKER_REGISTRY_HOST=

# Set this to an existent Docker container ID if you want to use an external volume as maven repository.
# If not set, the container will be created into build.sh
#MVN_REPO_CONTAINER_NAME=

# Set this to an existent Docker container ID if you want to use an external volume as stage-area repository.
# If not set, the contained will be created into build.sh
#DATA_CONTAINER_NAME=

# Platform used. Values: centos6, centos7 (default)
#PLATFORM=centos7

# pkg.storm repo directory - use in case of local
#PKG_REPO=/code/pkg.storm

# Use this branch of pkg.storm repo
PKG_TAG=nightly

# Set to 1 if you want to store rpms into stage area
PKG_STAGE_RPMS=1

# UMD release rpm URL
#UMD_REPO_RPM=http://repository.egi.eu/sw/production/umd/4/centos7/x86_64/updates/umd-release-4.1.3-1.el7.centos.noarch.rpm

# Set to 1 if you want to add build number on enabled rpms
#INCLUDE_BUILD_NUMBER=1

# Other variables used to set local git pkg repo
#STORM_FRONTEND_SERVER_PKG_REPO=/code/storm-frontend
#BUILD_REPO_STORM_GRIDFTP_DSI=/code/dsi
#BUILD_REPO_STORM_BACKEND_SERVER=/code/storm

# History of used DOCKER_ARGS:
#DOCKER_ARGS="--cap-add SYS_PTRACE"
#DOCKER_ARGS="-v ${HOME}/git/pkg.storm:/code/pkg.storm:ro --cap-add SYS_PTRACE"
# -v ${HOME}/git/storm-frontend:/code/storm-frontend:ro --cap-add SYS_PTRACE"

#PKG_TAG=$(git rev-parse --abbrev-ref HEAD)
